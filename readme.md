.dotfiles for my void-linux + gnome environment

---

> [!WARNING]
> the `mksymlinks.sh` script will forcibly replace any existing files

```bash
cd ~
git clone git@github.com:geoorgeous/.dotfiles.git
cd .dotfiles
bash ./mksymlinks.sh
```
